#include <stdio.h>

int main() {
    int dividend;
    int divisor;
    scanf("%d", &dividend);
    scanf("%d", &divisor);
    int remainder = dividend%divisor;
    printf ("%d",remainder);
    return 0;
}
